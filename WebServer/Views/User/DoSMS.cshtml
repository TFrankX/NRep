@using WebServer.Models.User
@model UserSMS
@{
    ViewData["Title"] = "Take Powerbank";
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Take Powerbank - A-Charger</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.css" />
    <link rel="stylesheet" href="~/css/acharger-theme.css" />
    <link rel="icon" type="image/svg+xml" href="~/favicon.svg" />
    <link rel="alternate icon" type="image/png" href="~/favicon.png" />
</head>
<body class="login-page">
    <div class="login-card fade-in">
        <div class="login-logo">
            <h1>
                <span class="logo-icon">
                    <i class="fas fa-bolt"></i>
                </span>
                A-Charger
            </h1>
        </div>

        <div class="login-title">
            <h2>Take Powerbank</h2>
            <p>Enter your phone number to receive a code</p>
        </div>

        <form asp-action="SendSMSCode" asp-controller="User" asp-route-returnUrl="@Model.ReturnUrl" asp-route-StationId="@Model.StationId" class="login-form">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger" style="display: @(ViewData.ModelState.ErrorCount > 0 ? "block" : "none")"></div>

            <div class="form-group-acharger">
                <label asp-for="PhoneNumber">
                    <i class="fas fa-phone me-1"></i>Phone Number
                </label>
                <input asp-for="PhoneNumber" class="form-control-acharger" placeholder="+357 99 123456" autofocus />
                <span asp-validation-for="PhoneNumber" class="text-danger-acharger" style="font-size: 0.875rem;"></span>
            </div>

            <button type="submit" class="btn-acharger btn-primary-acharger btn-login">
                <i class="fas fa-paper-plane"></i>
                Send SMS Code
            </button>

            <div class="d-grid gap-2 mt-3">
                <a asp-action="SendCancel" asp-controller="User" class="btn-acharger btn-secondary-acharger">
                    <i class="fas fa-times"></i>
                    Cancel
                </a>
            </div>
        </form>

        @if (Model.StationId > 0)
        {
            <div class="mt-4 p-3" style="background: var(--accent-light); border-radius: var(--border-radius-sm);">
                <small class="text-primary-acharger">
                    <i class="fas fa-charging-station me-1"></i>
                    Station ID: <strong>@Model.StationId</strong>
                </small>
            </div>
        }
    </div>

    <script src="~/lib/jquery/dist/jquery.js"></script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
</body>
</html>
